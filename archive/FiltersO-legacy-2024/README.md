# FiltersO Legacy Archive

This archive marks the retirement of the former `app/FiltersO/` directory.

- Any legacy filters recovered from older deployments should be placed here for
  reference only.
- The application autoloader and filter configuration must use `app/Filters/`
  exclusively.
- Do not restore `app/FiltersO/` in active code paths.
