# AiOps Task Run Artifacts

This folder contains machine-readable outputs produced by ops automation commands.

- `commands_lint.json`: generated by `php spark ops:lint:commands` and used in CI to verify command safety + docs coverage.
